\documentclass[aspectratio=169,t]{beamer}

% Settings
\def\exam{EBTEIS}
\def\duration{60} % Set the duration of the exam in minutes
\def\startAttendanceCheck{5} % Set the time for the start of the attendance check in minutes after the start of the exam (should always be in between 0 and \duration/2). If not specified, there will no marker on the timer for the attendance check.
\def\latestEarlyHandIn{45} % Set the latest time for the early hand in in minutes after the start of the exam (should always be in between \duration/2 and \duration). If not specified, there will no marker on the timer for the latest time of early hand in.
\def\pages{?} % Set the number of pages of the exam before using
%\def\cheatsheetAllowed{1} % Comment in if a cheat sheet is allowed. Set to 1 is only one-side of a DIN A4 paper is allowed to be used, set to 2 if a double-sided DIN A4 paper may be used.
%\def\examInspectionLink{https://www.studon.fau.de/cat5312611.html} % Comment in if an exam inspection link (replace the standard link if necessary) is available and the exam inspection is done directly after the grade is published
\def\german{1} % Comment in if the language of the exam is german (english is default)

% Import the presentation
\include{src/presentation}